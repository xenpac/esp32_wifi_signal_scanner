#
# This is a project Makefile. It is assumed the directory this Makefile resides in is a
# project subdirectory.
# The general ESP-IDF Framework mk file will be included.
# NOTE: you must export the PATH to ESP-IDF before using make:  export IDF_PATH=~/esp/esp-idf
#  Here just specify the project name!!!!
PROJECT_NAME := scanner

include $(IDF_PATH)/make/project.mk

